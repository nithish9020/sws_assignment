# Dependencies
node_modules/
.pnp
.pnp.js

# Builds / output
dist/
build/
.expo/
.expo-shared/
web-build/

# Env
.env
.env.local
.env.*.local
!.env.example

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Bun
bun.lock
# (keep bun.lock if you prefer reproducible installs — remove the line above to commit it)

# Drizzle generated SQL (keep migrations, ignore meta journal noise if desired)
# service/drizzle/

# Uploaded files when using the local-disk storage adapter (dev only)
service/uploads/

# Native / Expo
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
ios/
android/

# Editor / OS
.DS_Store
.vscode/
.idea/
*.tsbuildinfo
